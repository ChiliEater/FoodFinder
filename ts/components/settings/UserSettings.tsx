import { ScrollView, Text } from "react-native";

const UserSettings = () => {
    return (
        <ScrollView>
            <Text>Settings</Text>
        </ScrollView>
    )
};

export default UserSettings;