import { ScrollView, Text } from "react-native";

const Bookmarks = () => {
    return (
        <ScrollView>
            <Text>Bookmarks</Text>
        </ScrollView>
    )
};

export default Bookmarks;