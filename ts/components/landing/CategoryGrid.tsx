import React from 'react';
import { View } from 'react-native';

type CategoryGridProps = {
    children: React.ReactNode,
};

const ActivityGrid = (props: CategoryGridProps) => {
    return (
        <View>
            Hi this just text
            
        </View>
    );
};